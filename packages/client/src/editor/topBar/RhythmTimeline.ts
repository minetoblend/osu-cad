import {
  ContainerDrawable,
  ContainerDrawableOptions,
} from '../../framework/drawable/ContainerDrawable';

export class RhytmTimeline extends ContainerDrawable {
  constructor(options: ContainerDrawableOptions = {}) {
    super(options);
  }
}
