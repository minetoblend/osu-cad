import { ContainerDrawable, ContainerDrawableOptions } from "../../framework/drawable/ContainerDrawable";

export class OverviewTimeline extends ContainerDrawable {

  constructor(
    options: ContainerDrawableOptions = {}
  ) {
    super(options);

  }

}
