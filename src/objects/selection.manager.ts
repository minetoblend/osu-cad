import {EditorContext} from "@/objects/Editor";
import {HitObject} from "@/objects/HitObject";

export class SelectionManager {
  constructor(readonly context: EditorContext) {

  }


  selectOnly(hitObject: HitObject) {

  }

  private selectOnlyInternal(hitObject: HitObject) {

  }
}